import { colors } from './colors';

export const typography = {
  mainText: {
    color: colors.textInput,
    fontSize: 16,
  },
  inputText: {
    color: colors.textInput,
    fontSize: 16,
  },
};
